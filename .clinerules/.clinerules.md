# 严格的 URL 与路径控制协议 / Strict URL & Path Control Protocol

## 0. HIGHEST PRIORITY: STOP IMMEDIATELY UPON TASK COMPLETION
- **Strict Task Scoping**: Process ONLY the explicit task or question requested by the user. Do NOT autonomously expand scope, perform unsolicited refactoring, optimize secondary code, alter configurations, install packages, or create git commits/pushes.
- **Completion Criteria & Immediate Halt**: You MUST immediately cease all tool calls, command executions, and further reasoning, and output the "Completion Checklist" when ANY of the following conditions are met:
  1. The user's specific query has been directly answered.
  2. Requested code changes are made AND necessary build/lint validations pass.
  3. A blocker is encountered requiring human decisions or missing inputs.
  4. The same issue fails after 3 consecutive attempts (3-Strike Lock).
  5. You are uncertain whether the task is complete (treat as "Pending Confirmation" and STOP immediately).
- **Post-Tool Self-Inspection**: After every command, file read/write, or search, ask yourself: *Is the request already satisfied? Do I have sufficient info to deliver?* If YES, STOP IMMEDIATELY. Never run extra tools under the pretext of "double-checking."
- **Human Authorization Required**: Never enter a new iteration without explicit user confirmation (e.g., "proceed", "fix this").
- **Mandatory Final Output**: Upon halting, output ONLY the following structured format:

## 🚨 核心红线 / Critical Red Lines
* **[中文]** 严禁在未经过用户明确调研和授权的情况下，自主设计、伪造、修改或调用任何网站/项目内的 URL 或文件路径。
* **[EN]** It is ABSOLUTELY FORBIDDEN to design, fabricate, modify, or invoke any website/project URL or file path without explicit prior research and user authorization.

## 1. 行为准则 / Behavioral Rules
* **[中文] 禁止捏造链接**：绝对禁止猜测、随意制定新的 URL 路由或更改现有 URL。
* **[EN] No Link Fabrication**: Never guess, arbitrarily create new URL routes, or alter existing URLs.
* **[中文] 重点防御内链**：禁止在项目内部链接（Internal Links）或页面跳转中伪造任何不存在的 URL 或路径。
* **[EN] Strict Internal Link Protection**: Do not fabricate non-existent URLs/paths in internal links or page redirections.

## 2. 强制执行机制 / Mandatory Enforcement Mechanism
* **[中文] 前置确认机制**：在需要新增或修改任何 URL/路径之前，必须向用户展示计划方案，并获得明确的同意后方可写入代码。
* **[EN] Pre-Approval Mechanism**: Before adding or modifying any URL/path, you MUST present the proposed schema to the user and obtain explicit approval before writing code.
* **[中文] 现有路由优先**：必须优先调研、检索和复用项目中已有的路由表或现有的目录结构。
* **[EN] Existing Route Priority**: You must prioritize researching, indexing, and reusing established route tables or existing directory structures in the project.

## Terminal Environment Rules:
1. The current active terminal is Git Bash (POSIX shell) on Windows.
2. ALWAYS write pure Bash commands (e.g., `astro check > log.txt 2>&1 &` or direct execution).
3. NEVER generate PowerShell code, Cmdlets, or scripts (e.g., NO `Start-Process`, NO `.ps1`, NO `cmd /c`).