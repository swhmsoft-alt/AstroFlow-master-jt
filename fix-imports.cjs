const fs = require("fs"); const path = require("path"); const files = []; function walk(d) { for (const e of fs.readdirSync(d, {withFileTypes:true})) { const p = path.join(d, e.name); if (e.isDirectory() && e.name !== "node_modules") walk(p); else if (p.endsWith(".astro")) files.push(p); } } walk("src"); let fixed = 0; for (const f of files) { let c = fs.readFileSync(f, "utf8"); if (c.includes("t(") && !c.includes("useTranslations")) { const imp = "import { getLangFromUrl, useTranslations } from \"@i18n/utils\";\nconst lang = getLangFromUrl(Astro.url);\nconst t = useTranslations(lang);"; c = c.replace("---", "---\n" + imp); fs.writeFileSync(f, c, "utf8"); console.log("Fixed: " + f); fixed++; } } console.log("Total fixed:", fixed);
