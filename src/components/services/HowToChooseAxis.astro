---
/**
 * HowToChooseAxis.astro
 *
 * "How to Choose Your Axis Configuration" — 3-step guide with HowTo
 * JSON-LD schema. Targets Buyer Decision Chain stage 2 (solution exploration)
 * + AI citation (HowTo schema, numbered steps).
 *
 * i18n: t(key) with English fallback.
 */
import { getLangFromUrl, useTranslations } from '../../i18n/utils';

const currentLang = getLangFromUrl(Astro.url);
const t = useTranslations(currentLang);

interface Step {
  name: string;
  text: string;
}

const steps: Step[] = [
  { name: t('services.howto.step1.name'), text: t('services.howto.step1.text') },
  { name: t('services.howto.step2.name'), text: t('services.howto.step2.text') },
  { name: t('services.howto.step3.name'), text: t('services.howto.step3.text') },
];

const badge = t('services.howto.badge');
const sectionTitle = t('services.howto.sectionTitle');
const sectionLede = t('services.howto.sectionLede');

const howtoJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'HowTo',
  name: t('services.howto.sectionTitle'),
  description: t('services.howto.sectionLede'),
  totalTime: 'PT1M',
  step: steps.map((s, idx) => ({
    '@type': 'HowToStep',
    position: idx + 1,
    name: s.name,
    text: s.text,
  })),
};
---

<section class="relative py-24 overflow-hidden" style="background-color: var(--theme-bg);">
  <div class="absolute inset-0 opacity-[0.04]" aria-hidden="true">
    <div class="absolute inset-0" style="background-image: linear-gradient(var(--theme-primary) 1px, transparent 1px), linear-gradient(90deg, var(--theme-primary) 1px, transparent 1px); background-size: 80px 80px;"></div>
  </div>

  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center mb-12 max-w-3xl mx-auto">
      <div
        class="inline-flex items-center px-4 py-2 mb-5 text-sm font-semibold rounded-full border"
        style="background-color: color-mix(in srgb, var(--theme-primary) 10%, transparent); border-color: var(--theme-primary); color: var(--theme-primary);"
      >
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
        </svg>
        {badge}
      </div>
      <h2 class="text-4xl md:text-5xl font-bold mb-5 leading-tight" style="color: var(--theme-text);">
        {sectionTitle}
      </h2>
      <p class="text-lg leading-relaxed" style="color: color-mix(in srgb, var(--theme-text) 65%, transparent);">
        {sectionLede}
      </p>
    </div>

    <ol class="max-w-4xl mx-auto space-y-5">
      {steps.map((step, idx) => (
        <li
          class="rounded-2xl p-6 md:p-7 flex flex-col md:flex-row md:items-start gap-5"
          style="background-color: var(--theme-surface); border: 1px solid color-mix(in srgb, var(--theme-primary) 22%, transparent);"
        >
          <div
            class="w-14 h-14 rounded-xl flex items-center justify-center shrink-0 font-bold text-2xl"
            style="background: linear-gradient(135deg, var(--theme-primary), color-mix(in srgb, var(--theme-primary) 70%, #000)); color: #0F172A;"
          >
            {idx + 1}
          </div>
          <div class="flex-1">
            <h3 class="text-xl font-bold mb-2" style="color: var(--theme-text);">
              {step.name}
            </h3>
            <p class="text-sm md:text-base leading-relaxed" style="color: color-mix(in srgb, var(--theme-text) 75%, transparent);">
              {step.text}
            </p>
          </div>
        </li>
      ))}
    </ol>

    <div
      class="mt-10 max-w-4xl mx-auto rounded-xl p-6 md:p-7 flex flex-col md:flex-row md:items-center gap-4"
      style="background: linear-gradient(135deg, var(--theme-surface), color-mix(in srgb, var(--theme-primary) 6%, var(--theme-surface))); border: 1px solid color-mix(in srgb, var(--theme-primary) 25%, transparent);"
    >
      <p class="text-sm md:text-base flex-1" style="color: var(--theme-text);">
        {t('services.howto.cta')}
      </p>
      <a
        href="/rfq/"
        class="inline-flex items-center justify-center whitespace-nowrap px-6 py-3 rounded-lg font-bold text-sm transition-all duration-200 hover:-translate-y-0.5"
        style="background: linear-gradient(135deg, #ff8a3d 0%, #ff5a00 100%); color: #ffffff;"
      >
        {t('services.howto.ctaButton')}
        <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3" />
        </svg>
      </a>
    </div>
  </div>

  <script type="application/ld+json" set:html={JSON.stringify(howtoJsonLd)} />
</section>